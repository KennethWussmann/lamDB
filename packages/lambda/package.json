{
  "name": "@lamdb/lambda",
  "version": "0.1.0",
  "main": "build/lambda/src/index.js",
  "types": "build/lambda/src/index.d.ts",
  "repository": "https://github.com/KennethWussmann/lamDB",
  "scripts": {
    "prepack": "npm run build",
    "dev": "npm run build --watch",
    "build": "tsc",
    "test": "jest --maxWorkers=50% --passWithNoTests",
    "clean": "rm -rf ./build"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.188.0",
    "@lamdb/core": "*",
    "@prisma/engine-core": "^4.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.107"
  }
}
