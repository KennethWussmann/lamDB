{
  "name": "@lamdb/server",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --noEmit && ncc build src/server.ts -o build",
    "start": "ts-node src/server.ts"
  },
  "dependencies": {
    "@lamdb/core": "*",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.14"
  }
}
